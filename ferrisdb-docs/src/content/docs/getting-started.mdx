---
title: Getting Started
description: Get up and running with FerrisDB - build, test, and explore a real database
---

import { Aside, Card, CardGrid, Steps, Tabs, TabItem } from "@astrojs/starlight/components";

Welcome to FerrisDB! This guide will help you get the project running locally so you can explore,
learn, and contribute.

## Prerequisites

<CardGrid>
  <Card title="Rust 1.81+" icon="seti:rust">
    Install via [rustup.rs](https://rustup.rs/)
  </Card>

<Card title="Git" icon="github">
  For cloning the repository
</Card>

  <Card title="Basic CLI knowledge" icon="laptop">
    Comfortable with terminal commands
  </Card>
</CardGrid>

## Quick Start

<Steps>

1. **Clone the repository**

   ```bash
   git clone https://github.com/ferrisdb/ferrisdb.git
   cd ferrisdb
   ```

2. **Install Rust toolchain**

   ```bash
   rustup toolchain install stable
   rustup component add rustfmt clippy
   ```

3. **Build the project**

   ```bash
   cargo build --all
   ```

4. **Run tests**
   ```bash
   cargo test --all
   ```

</Steps>

<Aside type="tip">
  The first build will take a few minutes as Cargo downloads and compiles dependencies. Subsequent
  builds will be much faster!
</Aside>

## Project Structure

Understanding the codebase layout helps you navigate and contribute:

```text
ferrisdb/
├── ferrisdb-core/       # Common types and traits
├── ferrisdb-storage/    # Storage engine (LSM-tree, WAL, etc.)
├── ferrisdb-client/     # Client library (planned)
├── ferrisdb-server/     # Server implementation (planned)
├── ferrisdb-docs/       # This documentation site
├── guidelines/          # Development guidelines
└── tests/               # Integration tests
```

## Development Setup

### Essential Tools

<Tabs>
  <TabItem label="Required">

    ```bash
    # Rust toolchain
    rustup toolchain install stable
    rustup component add rustfmt clippy
    ```

  </TabItem>

  <TabItem label="Recommended">

    ```bash
    # Development tools
    cargo install cargo-watch cargo-nextest
    
    # Documentation tools (if contributing docs)
    npm install -g prettier
    ```

  </TabItem>

  <TabItem label="Optional">

    ```bash
    # Performance analysis
    cargo install flamegraph
    
    # Security audit
    cargo install cargo-audit
    
    # Dependency analysis
    cargo install cargo-tree
    ```

  </TabItem>
</Tabs>

### Environment Setup

```bash
# Enable debug logging
export RUST_LOG=debug
export RUST_BACKTRACE=1

# For detailed test output
export RUST_TEST_THREADS=1
```

## Common Tasks

### Running Tests

<CardGrid>
  <Card title="All tests" icon="rocket">
    ```bash cargo test --all ```
  </Card>

<Card title="Specific test" icon="magnifier">
  ```bash cargo test test_name ```
</Card>

<Card title="With output" icon="document">
  ```bash cargo test -- --nocapture ```
</Card>

  <Card title="Fast testing" icon="rocket">
    ```bash cargo nextest run --all ```
  </Card>
</CardGrid>

### Development Workflow

```bash
# Watch for changes and auto-run tests
cargo watch -x "test --all"

# Check code before committing
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
```

### Building Documentation

```bash
# Generate and open Rust docs
cargo doc --all --no-deps --open

# Serve the website locally
cd ferrisdb-docs
npm install
npm run dev
```

## Exploring the Code

### Start with Storage Engine

The storage engine is the heart of FerrisDB. Here's how to explore it:

1. **Read the WAL implementation**: `ferrisdb-storage/src/wal/`
   - Start with `mod.rs` for the overview
   - Look at `writer.rs` for durability guarantees
2. **Understand the MemTable**: `ferrisdb-storage/src/memtable/`

   - `skip_list.rs` - Lock-free concurrent data structure
   - See how writes are buffered in memory

3. **Explore SSTable format**: `ferrisdb-storage/src/sstable/`
   - `writer.rs` - How data is persisted to disk
   - `reader.rs` - Efficient lookups in sorted files

### Running Examples

```bash
# Create a sample SSTable
cargo run --example create_sstable

# Test WAL recovery
cargo run --example wal_recovery

# Benchmark skip list performance
cargo bench --bench skiplist_bench
```

## Learning Paths

<CardGrid>
  <Card title="Database Internals Path" icon="magnifier">
    1. Read [WAL article](/concepts/database-internals/wal/) 2. Run WAL tests and trace execution 3.
    Modify WAL to add metrics 4. Submit PR with your learnings
  </Card>

<Card title="Rust Learning Path" icon="seti:rust">
  1. Read [Ownership article](/concepts/rust-patterns/ownership/) 2. Find a `clone()` that could be
  avoided 3. Refactor to use references 4. Benchmark the improvement
</Card>

  <Card title="Contributing Path" icon="puzzle">
    1. Pick an issue labeled "good first issue" 2. Read relevant code and tests 3. Ask questions in
    the issue 4. Submit a draft PR early
  </Card>
</CardGrid>

## Debugging Tips

### Understanding Test Failures

```bash
# Run single test with backtrace
RUST_BACKTRACE=full cargo test test_name -- --nocapture

# Use println! debugging
println!("Value at this point: {:?}", value);

# Use dbg! macro (removes easily)
dbg!(&my_variable);
```

### Performance Investigation

```bash
# Run benchmarks
cargo bench

# Generate flamegraph (requires perf on Linux)
cargo flamegraph --bench bench_name

# Check binary size
cargo bloat --release
```

## Getting Help

<Aside type="tip">Remember: There are no stupid questions! We're all learning together.</Aside>

- 📖 Read the [FAQ](/project/faq/) for common questions
- 💬 Open an [issue on GitHub](https://github.com/ferrisdb/ferrisdb/issues)
- 🔍 Search existing issues - someone might have asked already
- 📝 Check the [blog](/blog/) for development insights

## Next Steps

Now that you have FerrisDB running:

1. **Run the test suite** to see everything working
2. **Pick a component** to explore deeply
3. **Make a small change** and see what happens
4. **Share what you learned** in an issue or PR

Welcome to the FerrisDB community! We're excited to learn with you. 🦀
